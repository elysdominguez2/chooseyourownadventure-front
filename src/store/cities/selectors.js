export const selectCities = (reduxState) => reduxState.city.cities;
export const selectCityById = (reduxState) => reduxState.city.cityDetails;
export const selectJourneyInfo = (reduxState) => reduxState.city.journeyInfo;
